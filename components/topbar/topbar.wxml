<view class="topbar" style="padding-top: {{statusBarHeight}}px; height: {{topbarHeight}}px;">
  <view wx:if="{{showBack}}" class="topbar-back" bindtap="goBack"></view>
  <view class="topbar-title">{{title}}</view>
</view>
